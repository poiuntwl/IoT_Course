? Проаналізувати, що виведе цей код і чому:

uint8_t a = 200;
uint8_t b = 100;
uint8_t sum = a + b;

! діапазон значень для цього типу даних -- 0-255. Тобто 255+1=256 буде 0, 255+2=257 буде 1 і так далі. Ця сума це 300. 300-255-1=44

? Коротко описати, де «живуть» змінні програми: стек / купа / .bss / flash.
! У стеку живуть локальні змінні, доки функція не поверне. У купі живуть змінні створені за допомогою динамічного виділення пам'яті, типу new чи malloc, доки не вивільнені. .bss -- там живуть глобальні та статичні змінні. Усі перебувають у RAM. Flash -- неволатильна пам'ять, 